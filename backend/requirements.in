# Checked 2026-07-02:
# https://pypi.org/pypi/aiogram/json latest is 3.29.1 and still requires
# aiohttp>=3.9.0,<3.15 plus redis[hiredis]>=6.2.0,<8.
aiogram==3.29.1
# Keep aiohttp below 3.15 until aiogram relaxes its runtime coupling; the
# checked latest https://pypi.org/pypi/aiohttp/json release is 3.14.1.
aiohttp>=3.14.1,<3.15
python-dotenv==1.2.2
pydantic==2.13.4
yookassa==3.11.0
httpx>=0.27.0
pydantic-settings==2.14.2
email-validator==2.3.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
# Checked 2026-07-02: https://pypi.org/pypi/aiocryptopay/json latest is 0.4.8
# and still caps certifi at <2024 (metadata-only). The Dockerfile force-upgrades
# certifi after install and pip-audit ignores PYSEC-2024-230 in CI until
# aiocryptopay relaxes this upstream pin.
aiocryptopay==0.4.8
certifi<2024
PyJWT[crypto]==2.13.0
Pillow==12.2.0
# Checked 2026-07-02: https://pypi.org/pypi/redis/json latest is 8.0.1, but
# aiogram 3.29.1 still caps redis[hiredis] at <8 for RedisStorage.
redis>=7.4.1,<8
